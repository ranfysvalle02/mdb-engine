# Embeddings

The embedding service generates vector embeddings for text, supporting multiple providers (OpenAI, Azure OpenAI) with automatic chunking and storage.

## EmbeddingService

::: mdb_engine.embeddings.service.EmbeddingService
    options:
      show_root_heading: true
      members_order: source

## Factory Functions

::: mdb_engine.embeddings.dependencies.get_embedding_service_for_app
