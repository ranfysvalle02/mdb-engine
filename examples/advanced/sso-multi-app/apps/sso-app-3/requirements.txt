# Requirements for SSO App 3 (AI Chat with OSI Family Model)
# Install mdb-engine from source (NOT published to PyPI):
#   pip install -e "../../../../..[osi,ai]"

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
jinja2>=3.1.2
python-multipart>=0.0.6
bcrypt>=4.0.1
pydantic>=2.0.0
python-dateutil>=2.8.2
python-dotenv>=1.0.0
pymongo>=4.0.0

# Memory service (Mem0)
mem0ai>=0.1.0

# AI/LLM Dependencies (install at least one provider)
litellm>=1.0.0
# OpenAI (recommended)
openai>=1.12.0
# Anthropic (alternative)
anthropic>=0.18.0
# Google Generative AI
google-genai>=0.1.0
# Ollama (for local/self-hosted)
# Note: Ollama uses OpenAI-compatible API, so openai package works

# Demo seeding script
aiohttp>=3.9.0

# Semantic text splitting for raw content chunking
semantic-text-splitter>=0.2.0

# File processing libraries for document upload support
python-docx>=1.1.0
pypdf>=3.17.0
pandas>=2.0.0
openpyxl>=3.1.0
