# Requirements for Interactive RAG Example
# Install mdb-engine from source (NOT published to PyPI):
#   pip install -e "../../..[ai]"

python-dotenv>=1.0.0

# Web framework
uvicorn[standard]>=0.24.0
jinja2>=3.1.0
python-multipart>=0.0.6

# LLM and Embedding dependencies
openai>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
tenacity>=8.0.0
semantic-text-splitter>=0.9.0

# RAG dependencies (optional - for document processing and web search)
ddgs>=0.1.0
docling>=1.0.0
requests>=2.31.0
