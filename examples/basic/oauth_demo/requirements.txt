# Requirements for OAuth Demo
# Install mdb-engine from source (NOT published to PyPI):
#   pip install -e "../../..[oauth]"

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0
python-multipart>=0.0.6

# OAuth support (via mdb-engine[oauth])
authlib>=1.3.0
httpx>=0.27.0
