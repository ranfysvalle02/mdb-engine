# Requirements for Parallax Example
# Install mdb-engine from source (NOT published to PyPI):
#   pip install -e "../../..[ai]"

# Core dependencies
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
httpx>=0.24.0
requests>=2.31.0
pyyaml>=6.0
