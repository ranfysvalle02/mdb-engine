# Requirements for ChitChat Example
# Install mdb-engine from source (NOT published to PyPI):
#   pip install -e "../../..[ai]"

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
jinja2>=3.1.2
python-multipart>=0.0.6
bcrypt>=4.0.1
pydantic>=2.0.0
python-dateutil>=2.8.2
python-dotenv>=1.0.0
pymongo>=4.0.0

# Memory service (Custom MongoDB Atlas Vector Search)
# No additional dependencies needed - uses mdb-engine's built-in memory service

# Authorization (Casbin)
casbin>=1.35.0
casbin-motor-adapter>=0.1.0

# AI/LLM Dependencies (install at least one provider)
# OpenAI (recommended)
openai>=1.12.0
# Anthropic (alternative)
anthropic>=0.18.0
# Ollama (for local/self-hosted)
# Note: Ollama uses OpenAI-compatible API, so openai package works

# Demo seeding script
aiohttp>=3.9.0

# Test script dependencies
requests>=2.31.0
