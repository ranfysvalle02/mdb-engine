# Requirements for OSO Hello World Example
# Install mdb-engine from source with OSO support (NOT published to PyPI):
#   pip install -e "../../..[oso]"

python-dotenv>=1.0.0

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
jinja2>=3.1.0
